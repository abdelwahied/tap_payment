name: 'Tap Payment test support'
type: module
description: 'Replaces the Tap HTTP transport with a scriptable stub so tests never touch the network.'
package: Testing
core_version_requirement: ^10.3 || ^11
dependencies:
  - tap_payment:tap_payment
